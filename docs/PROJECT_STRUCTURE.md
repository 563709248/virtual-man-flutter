# 项目结构

- `lib/config`：运行时 API 地址配置。
- `lib/models`：聊天、关系、记忆和通知的类型模型。
- `lib/services`：Dio 客户端、鉴权会话和后端接口封装。
- `lib/pages`：登录注册、应用导航、聊天和陪伴功能页面。
- `test`：组件与服务测试。

Android 和 iOS 使用同一套 Flutter/Dart UI，不在页面层写平台分支。
