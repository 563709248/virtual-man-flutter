# Changelog

## Unreleased

- 新增 6 套预设虚拟男友（温柔学长、冷酷总裁、阳光运动、文艺青年、邻家男孩、狼系男友），内置 AI 生成的 CG 写实风 JPG 头像（Pollinations/FLUX 生成，无真人肖像权问题）；角色创建页新增预设选择器，选中自动填充名称、头像、性格、背景与 Prompt 设定，仍可编辑；头像支持点击大图预览（双指缩放）；每个预设附带 Hunyuan3D 图生 3D 模型（assets/models/bf_*.glb，约 0.5MB），预览中可切换 3D 查看（旋转缩放）；聊天页头部新增 3D 按钮，可在悬浮面板中展示角色 3D 模型，支持拖动移位与拖拽缩放大小。
- 角色支持 `modelUrl` 字段，为 3D 模型资源预留；头像字段新增 `preset:` 内置资源方案，聊天页与表单预览统一由 `CharacterAvatar` 渲染。
- 创建订单不再携带 userId（后端改由登录态确定）；订单完成确认为管理员专用入口。
- 对齐后端网关路径，修正认证、聊天、记忆和通知接口的重复服务前缀。
- 新增当前认证用户的角色加载与切换，聊天、羁绊和记忆改为使用所选角色。
- 新增主动陪伴提醒开关，与后端持久化偏好同步。
- 聊天回复改为 SSE 分片实时渲染；中断时保留已接收内容。
- 新增用户资料编辑页，改用当前认证用户范围的资料接口。
- 通知改用当前认证用户范围的接口，支持仅看未读筛选。
- 接入认证用户范围的聊天历史游标分页与长期记忆查看、编辑、删除。
- 移除开发页面中的固定用户 ID，改为从登录 JWT 读取兼容接口所需的用户标识。
- 重构为 Material 3 陪伴应用壳，支持 Android 与 iOS。
- 集成 Bearer 会话、关系成长、长期记忆和通知接口。
- 改进聊天输入、加载与错误状态。
